# Password protection (optional in local dev; required on Vercel production)
# Generate hash locally: npm run hash-password (paste line as printed; $ must be \\$)
AUTH_PASSWORD_HASH=

# Session signing secret — random 32+ characters (e.g. openssl rand -base64 32)
AUTH_SECRET=
